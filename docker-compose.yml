services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/config/.env
    depends_on:
      - mongo
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - goodsly-mongo:/data/db
volumes:
  goodsly-mongo:
